{{>_navbar}}

{{#each erros}}
  <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<div class="card">
  <div class="card-body">
      <form action="/{{loja}}/admin/pagamentos/nova" method="post">
    <label for="nome">Forma de pagamento: </label>
      <input class="form-control" type="text" id="nome" name="nome" placeholder="nome do pagamento">
    <br>

    <button type="submit" class="btn btn-success">Incluir forma de pagamento</button>
  </form>
  </div>
</div>