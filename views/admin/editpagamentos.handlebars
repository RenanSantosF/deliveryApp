{{>_navbar}}
{{#each erros}}
  <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<div class="card">
  <div class="card-body">
    <form action="/{{loja}}/admin/pagamentos/edit" method="post">
    <input type="hidden" name="id" value="{{pagamento._id}}">
      <label for="nome">Forma de pagamento: </label>
        <input class="form-control" type="text" id="nome" name="nome" value="{{pagamento.nome}}">
      <br>

      <button type="submit" class="btn btn-success">Editar forma de pagamento</button>
    </form>
  </div>
</div>