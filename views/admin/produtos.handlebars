{{>_navbar}}


<div class="body">
  <h2 class="title">Produtos</h2>
  <div class="btnNova">
    <a href="/{{loja}}/admin/produtos/add">
      <button class="button">
        <span>
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24"><path fill="none" d="M0 0h24v24H0z"></path><path fill="currentColor" d="M11 11V5h2v6h6v2h-6v6h-2v-6H5v-2z"></path></svg>
          Novo produto
        </span>
      </button>
    </a>
  </div>

{{!-- {{#each produtos}}
  <div class="card">

    <div>
      <div class="containerContent">
        <div class="imgProduto">
          <img src="/uploads/{{imgProduto}}" alt="">
        </div>

        <div>
          <div class="titulo">
            <h2>{{titulo}}</h2>
          </div>
          <div class="nome">
            <h2>{{this.categoria.nome}}</h2>
          </div>
          <div class="taxa">
            <h2>R$ {{preco}}</h2>
          </div>
        </div>



      </div>


    </div>

    <div class="openModal" id="openModal">
      <img class="abre" src="/img/3pontos.png" />
    </div>

    <div class="modal">
      <a href="/{{nomeLoja}}/admin/produtos/edit/{{_id}}">
        <button class="btn edit">Editar</button>
      </a>
      <form action="/{{nomeLoja}}/admin/produtos/deletar" method="post">
        <input type="hidden" name="id" value="{{_id}}" />
        <button class="btn delete" type="submit">Deletar</button>
      </form>
      <form action="/{{nomeLoja}}/admin/produtos/disponibilidade" method="post">
        <input type="hidden" name="id" value="{{_id}}" />  <!-- Este input estava comentado -->
        {{#if disponivel}}
          <button class="btnDisponibilidade indisponivel" type="submit">Tornar indisponível</button>
        {{else}}
          <button class="btnDisponibilidade disponivel" type="submit">Tornar disponível</button>
        {{/if}}
      </form>
    </div>
    
  </div>
{{else}}
<p>Nenhum produto registrado!</p>
{{/each}} --}}


{{#each produtosPorCategoria}}
  <div class="categoria">
    <h2>{{@key}}</h2>
    {{#each this}}
      <div class="card">
        <div>
          <div class="containerContent">
            <div class="imgProduto">
              <img src="/uploads/{{imgProduto}}" alt="">
            </div>
            <div>
              <div class="titulo">
                <h2>{{titulo}}</h2>
              </div>
              {{!-- <div class="nome">
                <h2>{{this.categoria.nome}}</h2>
              </div> --}}
              <div class="taxa">
                <h2>R$ {{preco}}</h2>
              </div>
            </div>
          </div>
        </div>
        <div class="openModal" id="openModal">
          <img class="abre" src="/img/3pontos.png" />
        </div>
        <div class="modal">
          <a href="/{{nomeLoja}}/admin/produtos/edit/{{_id}}">
            <button class="btn edit">Editar</button>
          </a>
          <form action="/{{nomeLoja}}/admin/produtos/deletar" method="post">
            <input type="hidden" name="id" value="{{_id}}" />
            <button class="btn delete" type="submit">Deletar</button>
          </form>
          <form action="/{{nomeLoja}}/admin/produtos/disponibilidade" method="post">
            <input type="hidden" name="id" value="{{_id}}" />
            {{#if disponivel}}
              <button class="btnDisponibilidade indisponivel" type="submit">Tornar indisponível</button>
            {{else}}
              <button class="btnDisponibilidade disponivel" type="submit">Tornar disponível</button>
            {{/if}}
          </form>
        </div>
      </div>
    {{/each}}
  </div>
{{else}}
  <p>Nenhum produto registrado!</p>
{{/each}}
