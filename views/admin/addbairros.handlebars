{{>_navbar}}

{{#each erros}}
  <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<div class="card">
  <div class="card-body">
      <form action="/{{loja}}/admin/bairros/nova" method="post">
    <label for="nome">Bairro: </label>
      <input class="form-control" type="text" id="nome" name="nome" placeholder="nome do bairro">
    <label for="nome">Taxa de entrega: </label>
      <input class="form-control" type="text" id="taxa" name="taxa" placeholder="Valor da entrega">
    <br>

    <button type="submit" class="btn btn-success">Incluir bairro</button>
  </form>
  </div>
</div>